[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "personal-ai-connectors"
dynamic = ["version"]
description = "External service connectors for Personal AI"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
  "personal-ai-core",
]

[tool.hatch.version]
path = "personal_ai_connectors/__init__.py"
